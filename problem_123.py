# write a python program to get degree angle from the user and then converted to radian angle =>
import math 
degree_angle = float(input("please enter the degree angle is = "))
print("the degree angle is = "+str(degree_angle)+"deg")
radian_angle = math . radians(degree_angle)
print("the radian angle is = "+str(radian_angle)+"rad")